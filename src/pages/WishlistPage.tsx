import { PlaceholderPage } from './PlaceholderPage';

export function WishlistPage() {
  return (
    <PlaceholderPage
      title="Wishlist"
      description="Your saved products and price alerts. Requires an account (added in a later phase)."
    />
  );
}
